import { useSelector as useSel } from "react-redux"

/**
 * @type {import('react-redux').TypedUseSelectorHook<import('./typings').AppState>}
 */
const useSelector = useSel

export default useSelector
